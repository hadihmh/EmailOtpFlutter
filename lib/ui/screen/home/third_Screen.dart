import 'package:flutter/material.dart';

class ThirtScreen extends StatelessWidget {
  const ThirtScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey[300],
      child: const Center(child: Text("ThirtScreen")),
    );
  }
}
